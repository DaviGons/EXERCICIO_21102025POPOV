# Dependências do Node.js
node_modules

# Arquivos de log
npm-debug.log*
yarn-error.log

# Pasta de compilação do TypeScript (que definimos no tsconfig.json)
dist

# Dados locais do Docker (a pasta que o 'volumes' criou)
pgdata

# Arquivos de sistema
.DS_Store
Thumbs.db